import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  root: {
    maxWidth: "100%", //480
    backgroundImage: " url('./img/humaaans.png')",
    height: "100vh",
  },
}));
